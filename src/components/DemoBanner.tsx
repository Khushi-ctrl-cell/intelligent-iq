export default function DemoBanner() {
  return (
    <div className="w-full px-4 py-2 bg-primary/10 border-b border-primary/30 text-center">
      <p className="text-xs font-semibold text-primary tracking-wide">
        DEMO MODE ENABLED — Admin access granted for evaluation
      </p>
    </div>
  );
}
